" vimrc.local
" " These commands will be run after all other configuration and plugins

colorscheme gruvbox

au FileType yaml set fdm=indent

"Remap the leader key
 :let mapleader = ','

 " yank to clipboard alias
 vnoremap <leader>y "*y
